package com.digitalid.domain;

// used to define which attributes are immutable/mutable

public enum IdentityAttribute {
    ID,
    NAME,
    GENDER,
    DATE_OF_BIRTH,
    NATIONALITY,
    ADDRESS
}
